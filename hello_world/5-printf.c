#include <stdio.h>

/**
 * main - Using printf in mi c's programs
 *
 * Writing in Linux but with c
 *
 * Return: Zero on success
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
