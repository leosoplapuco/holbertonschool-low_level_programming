#include <stdio.h>
#include "main.h"

/**
*_islower(int c) - Script than print the values one or zero
*
*num: value
*
* Return: zero on success.
*/

int _islower(int c)
{
	int c;

	if (c = 97; c <= 122; c++)
	{
		return (1);
	}else
	{
		return (0);
	}
}
